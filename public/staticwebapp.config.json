{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://spottrack-platform-os.azurewebsites.net/api/*"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*", "*.{css,scss,js,png,jpg,svg,ico,json}"]
  }
}
